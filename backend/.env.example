# 基础配置
NODE_ENV=development
PORT=3001
MONGODB_URI=mongodb://localhost:27017/botskill
JWT_SECRET=your-super-secret-jwt-key

# 前端地址（OAuth 回调重定向）
FRONTEND_URL=http://localhost:5173

# 后端地址（用于 OAuth callback URL，生产环境需改为实际域名）
BACKEND_URL=http://localhost:3001

# 站点 URL（用于 sitemap.xml / robots.txt 中的绝对地址，生产环境需改为 https://yourdomain.com）
# SITE_URL=https://yourdomain.com

# Google OAuth（在 https://console.cloud.google.com/ 创建 OAuth 2.0 凭据）
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# 可选，不填则使用 BACKEND_URL + /api/auth/google/callback
# GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# GitHub OAuth（在 https://github.com/settings/developers 创建 OAuth App）
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# 可选，不填则使用 BACKEND_URL + /api/auth/github/callback
# GITHUB_CALLBACK_URL=http://localhost:3001/api/auth/github/callback
